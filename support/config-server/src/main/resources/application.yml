---
server.port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/coolbeevip/config-repo
  boot:
    admin:
      url: http://localhost:8762
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/